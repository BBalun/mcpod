export function parsePseudoHtml(string: string) {
  string = string.replace(/\[/g, "<");
  string = string.replace(/\]/g, ">");
  string = string.replace(/\\\</g, "[");
  string = string.replace(/\\\>/g, "]");
  string = string.replace(/\{amp\}/g, "&amp;");
  string = string.replace(/\~/g, "&nbsp;");
  string = string.replace(/\{tilda\}/g, "~");
  string = string.replace(/\{quot\}/g, '"');
  string = string.replace(/\{alpha\}/g, "&alpha;");
  string = string.replace(/\{beta\}/g, "&beta;");
  string = string.replace(/\{gamma\}/g, "&gamma;");
  string = string.replace(/\{delta\}/g, "&delta;");
  string = string.replace(/\{epsilon\}/g, "&epsilon;");
  string = string.replace(/\{zeta\}/g, "&zeta;");
  string = string.replace(/\{eta\}/g, "&eta;");
  string = string.replace(/\{theta\}/g, "&theta;");
  string = string.replace(/\{iota\}/g, "&iota;");
  string = string.replace(/\{kappa\}/g, "&kappa;");
  string = string.replace(/\{lambda\}/g, "&lambda;");
  string = string.replace(/\{mu\}/g, "&mu;");
  string = string.replace(/\{nu\}/g, "&nu;");
  string = string.replace(/\{xi\}/g, "&xi;");
  string = string.replace(/\{omicron\}/g, "&omicron;");
  string = string.replace(/\{pi\}/g, "&pi;");
  string = string.replace(/\{rho\}/g, "&rho;");
  string = string.replace(/\{sigma\}/g, "&sigma;");
  string = string.replace(/\{tau\}/g, "&tau;");
  string = string.replace(/\{upsilon\}/g, "&upsilon;");
  string = string.replace(/\{phi\}/g, "&phi;");
  string = string.replace(/\{chi\}/g, "&chi;");
  string = string.replace(/\{psi\}/g, "&psi;");
  string = string.replace(/\{omega\}/g, "&omega;");
  string = string.replace(/\{Alpha\}/g, "&Alpha;");
  string = string.replace(/\{Beta\}/g, "&Beta;");
  string = string.replace(/\{Gamma\}/g, "&Gamma;");
  string = string.replace(/\{Delta\}/g, "&Delta;");
  string = string.replace(/\{Epsilon\}/g, "&Epsilon;");
  string = string.replace(/\{Zeta\}/g, "&Zeta;");
  string = string.replace(/\{Eta\}/g, "&Eta;");
  string = string.replace(/\{Theta\}/g, "&Theta;");
  string = string.replace(/\{Iota\}/g, "&Iota;");
  string = string.replace(/\{Kappa\}/g, "&Kappa;");
  string = string.replace(/\{Lambda\}/g, "&Lambda;");
  string = string.replace(/\{Mu\}/g, "&Mu;");
  string = string.replace(/\{Nu\}/g, "&Nu;");
  string = string.replace(/\{Xi\}/g, "&Xi;");
  string = string.replace(/\{Omicron\}/g, "&Omicron;");
  string = string.replace(/\{Pi\}/g, "&Pi;");
  string = string.replace(/\{Rho\}/g, "&Rho;");
  string = string.replace(/\{Sigma\}/g, "&Sigma;");
  string = string.replace(/\{Tau\}/g, "&Tau;");
  string = string.replace(/\{Upsilon\}/g, "&Upsilon;");
  string = string.replace(/\{Phi\}/g, "&Phi;");
  string = string.replace(/\{Chi\}/g, "&Chi;");
  string = string.replace(/\{Psi\}/g, "&Psi;");
  string = string.replace(/\{Omega\}/g, "&Omega;");
  string = string.replace(/\{Aring\}/g, "&Aring;");

  return string;
}
